//const express = require('express');
//const router = express.Router();

//router.get("/api/hoteles", (req, res) => {
  //try {
    //const sqlQuery = 'SELECT _id, Nombre, Categoría, Calificaciones, Precio, Fotos FROM ';
  //} catch (error) {
    //res.status(400).send(error.message);
  //}
  
  
  //res.status(200).json({
    //Nombre: req.params.Nombre,
    //Categoría: req.params.Categoría,
    //Calificaciones: req.params.Calificaciones,
    //Precio: req.params.Precio,
    //Fotos: req.params.Fotos
  //})
//});

//module.exports = router;